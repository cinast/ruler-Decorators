```mermaid
flowchart TD
    A[用户应用 PropertyProxy] --> B[标记属性模式为'proxy']
    A --> C[用户应用其他规则装饰器<br>如Int, minimum]
    C --> D[添加对应的setter/getter处理器<br>到Descriptor存储]

    B --> E[类实例化]
    E --> F{是否已启用ClassProxy?}
    F -- 是 --> G[由ClassProxy统一管理<br>跳过PropertyProxy创建]
    F -- 否 --> H[创建PropertyProxy]

    H --> I[Proxy对象包装原实例]
    I --> J[配置get陷阱]
    I --> K[配置set陷阱]

    L[外部代码访问属性] --> M{访问被标记属性?}
    M -- 是 --> N[触发Proxy陷阱]
    M -- 否 --> O[直接访问属性]

    N --> P[get陷阱: 调用$applyGetterHandlers]
    N --> Q[set陷阱: 调用$applySetterHandlers]

    P & Q --> R[执行Descriptor中对应的<br>所有处理器链]
    R --> S[返回处理后的值]
```
